import AdminPanel from "@/components/AdminPanel";

export const metadata = {
  title: "Submissions Panel — Mind Over Matter",
  robots: { index: false, follow: false },
};

export default function PanelPage() {
  return <AdminPanel />;
}
